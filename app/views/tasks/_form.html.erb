<%= form_with(model: task, local: true) do |f| %>
  <%= f.label :content, 'タスク名' %>
  <%= f.text_field :content %>
  
  <%= f.label :status, 'タスク内容' %>
  <%= f.text_field :status %>
  
  <%= f.submit '新規作成' %>
<% end %>